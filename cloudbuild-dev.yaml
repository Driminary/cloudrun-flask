steps:
  # build the container image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/cr-flask-dev', '.']
  # push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/cr-flask-dev']
  # Deploy container image to Cloud Run
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['beta', 'run', 'deploy', 'cr-flask-dev', '--set-env-vars', 'appenv=Development', '--image', 'gcr.io/$PROJECT_ID/cr-flask-dev', '--region', 'us-central1']
images:
- gcr.io/$PROJECT_ID/cr-flask-dev
